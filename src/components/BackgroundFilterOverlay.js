import React from 'react';
import './BackgroundFilterOverlay.css'; // CSS we'll add next

const BackgroundFilterOverlay = () => {
  return <div className="background-filter-overlay" />;
};

export default BackgroundFilterOverlay;
